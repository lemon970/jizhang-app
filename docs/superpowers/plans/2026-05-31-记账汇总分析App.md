# 记账汇总分析 App 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做出一个能在手机上运行的网页 App（PWA），每月导入支付宝 CSV + 微信 xlsx 账单，自动合并去重、分类，用图表看清钱花在哪——全程数据不出本机。

**Architecture:** 纯前端单页应用，零构建（HTML+CSS+原生 JS ES Module）。架构上把"纯逻辑"与"I/O/UI"分层：解析→归一→去重→分类→统计这条数据流全是纯函数，用 Node 内置测试守护；文件读取(GBK解码/xlsx)、IndexedDB 存储、Chart.js 渲染、DOM 交互在浏览器里实测验收。第三方库下载到 `js/vendor/` 本地引用，保证离线可用。

**Tech Stack:** HTML/CSS/原生 JS(ES Module)；PapaParse(CSV)；SheetJS/xlsx(Excel)；Chart.js(图表)；IndexedDB(存储)；浏览器内置 TextDecoder('gbk')(解码)；测试用 Node 18+ 内置 `node:test`+`node:assert`；部署 Netlify/GitHub Pages。

**前置要求：** 开发机需有 Node 18+（用于跑纯逻辑测试，命令 `node --version` 确认）。App 本身运行只需浏览器。

**隐私红线：** 两份真实账单文件（`*.csv`/`*.xlsx`）已被 `.gitignore` 排除，绝不进版本库。测试一律用合成假数据，不使用真实账单做 fixture。真实文件仅在浏览器手动验收阶段由作者本地导入。

---

## 文件结构

```
app/
  package.json               # {"type":"module"} —— 让 Node 把 .js 当 ES Module 跑测试
  index.html                 # 应用外壳，加载 css 与 app.js
  css/style.css              # 样式（手机竖屏优先）
  js/
    vendor/                  # 本地第三方库（离线可用）
      papaparse.min.js
      xlsx.full.min.js
      chart.umd.min.js
    decode.js                # decodeGbk(buffer)->string
    parse-alipay.js          # parseAlipayCsv(text)->RawAlipay[]
    parse-wechat.js          # readWechatFile(file)->rows ; parseWechatRows(rows)->RawWechat[]
    normalize.js             # normalizeAlipay/normalizeWechat -> Transaction
    dedup.js                 # mergeTransactions(existing,incoming) ; isCountableExpense(txn)
    classify.js              # classify(txn,memory) ; KEYWORD_RULES ; ALIPAY_CATEGORY_MAP
    aggregate.js             # summarize(transactions,range)->汇总
    storage.js               # IndexedDB 读写 + 备份导出/导入
    charts.js                # renderCategoryPie / renderTrend
    app.js                   # UI 装配：导入流程/仪表盘/明细/手动归类
  tests/
    fixtures.js              # 合成假数据（绝不含真实账单）
    parse-alipay.test.js
    parse-wechat.test.js
    normalize.test.js
    dedup.test.js
    classify.test.js
    aggregate.test.js
  manifest.webmanifest       # PWA 清单
  sw.js                      # service worker（离线缓存）
  icons/icon-192.png icon-512.png
```

**统一数据模型 `Transaction`（贯穿全程，字段名固定）：**
```
{
  id:           string,   // 去重唯一键：订单号 trim 后
  source:       '支付宝' | '微信',
  datetime:     string,   // ISO 格式 "2026-05-30T20:17:08"
  amount:       number,   // 正数
  direction:    '支出' | '收入' | '中性',
  counterparty: string,   // 交易对方
  description:  string,   // 商品说明
  rawCategory:  string,   // 原始分类（支付宝交易分类 / 微信交易类型）
  myCategory:   string,   // 统一分类（见 classify）
  status:       string,   // 交易状态原文
  payMethod:    string,   // 支付方式原文
  note:         string    // 备注
}
```

---

## 阶段 0：项目骨架

### Task 1: 建立可运行的空壳页面

**Files:**
- Create: `index.html`
- Create: `css/style.css`
- Create: `js/app.js`

- [ ] **Step 1: 创建 package.json 与 index.html**

`package.json`（关键：`type:module` 让 Node 把源码/测试的 `import/export` 当 ES Module 运行）：
```json
{
  "name": "jizhang-app",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": { "test": "node --test" }
}
```

`index.html`：
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>我的记账</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <header><h1>我的记账</h1></header>
  <main id="app">
    <p id="placeholder">正在加载…</p>
  </main>
  <script type="module" src="js/app.js"></script>
</body>
</html>
```

- [ ] **Step 2: 创建 css/style.css（手机竖屏基础样式）**

```css
* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: -apple-system, "PingFang SC", "Microsoft YaHei", sans-serif;
  background: #f5f6f8; color: #222; line-height: 1.5; }
header { background: #2c7be5; color: #fff; padding: 16px; text-align: center; }
header h1 { font-size: 20px; }
main { max-width: 720px; margin: 0 auto; padding: 16px; }
button { font-size: 15px; padding: 10px 16px; border: none; border-radius: 8px;
  background: #2c7be5; color: #fff; cursor: pointer; }
button:active { opacity: .85; }
```

- [ ] **Step 3: 创建 js/app.js（确认脚本加载成功）**

```javascript
function main() {
  const el = document.getElementById('placeholder');
  el.textContent = '空壳已就绪 ✓ 下一步将接入账单导入';
}
main();
```

- [ ] **Step 4: 浏览器验证**

操作：用本地静态服务器打开页面（推荐 `npx serve .` 或 VS Code Live Server；不要用 file:// 因后续 PWA/模块需要 http）。
预期：页面顶部蓝色标题"我的记账"，正文显示"空壳已就绪 ✓ ..."。

- [ ] **Step 5: 提交**

```bash
git add package.json index.html css/style.css js/app.js
git commit -m "feat: 项目空壳页面 + package.json(type:module)，确认资源与模块加载"
```

---

### Task 2: 下载第三方库到本地（离线可用）

**Files:**
- Create: `js/vendor/papaparse.min.js`
- Create: `js/vendor/xlsx.full.min.js`
- Create: `js/vendor/chart.umd.min.js`

- [ ] **Step 1: 下载三个库到 js/vendor/**

```bash
mkdir -p js/vendor
curl -L -o js/vendor/papaparse.min.js   https://cdn.jsdelivr.net/npm/papaparse@5.4.1/papaparse.min.js
curl -L -o js/vendor/xlsx.full.min.js    https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js
curl -L -o js/vendor/chart.umd.min.js    https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js
```

- [ ] **Step 2: 校验文件非空且为 JS**

Run: `ls -l js/vendor/ && head -c 80 js/vendor/papaparse.min.js`
预期：三个文件大小均 > 10KB，head 输出为 JS 代码（非 HTML 错误页）。

- [ ] **Step 3: 提交**

```bash
git add js/vendor/
git commit -m "chore: 本地引入 PapaParse/SheetJS/Chart.js，保证离线运行"
```

---

## 阶段 1：解析与列出

> 本阶段把两种文件解析成统一 Transaction 并在页面表格列出。纯解析逻辑用 node:test 守护（合成假数据）；GBK 解码与 xlsx 读取在浏览器用真实文件验收。

### Task 3: 合成测试 fixture（不含真实数据）

**Files:**
- Create: `tests/fixtures.js`

- [ ] **Step 1: 写入合成假数据**

```javascript
// 全部为虚构数据，结构仿真实账单，安全可提交
// 支付宝 CSV 文本：前 23 行说明 + 第 24 行表头 + 数据行（UTF-8，解码后形态）
export const ALIPAY_CSV_TEXT = [
  '------------------------------------------------------------------------------------',
  '支付宝账户：test@example.com',
  '起始时间：[2026-05-01 00:00:00]    终止时间：[2026-05-31 23:59:59]',
  ...Array(20).fill('说明行占位'),
  '交易时间,交易分类,交易对方,对方账号,商品说明,收/支,金额,收/付方式,交易状态,交易订单号,商家订单号,备注,',
  '2026-05-30 20:17:08,餐饮美食,测试商户A,a@x.com,午餐套餐,支出,21.92,中国银行,交易成功,ALI0001\t,M0001\t,,',
  '2026-05-29 09:00:00,交通出行,测试地铁,b@x.com,地铁行程,支出,4.00,余额宝,交易成功,ALI0002\t,M0002\t,,',
  '2026-05-28 12:00:00,收入,某人,c@x.com,转账,不计收支,500.00,余额,交易成功,ALI0003\t,M0003\t,,',
  '2026-05-27 18:00:00,餐饮美食,测试商户A,a@x.com,被取消的单,支出,30.00,中国银行,交易关闭,ALI0004\t,M0004\t,,',
].join('\n');

// 微信 SheetJS 提取后的二维数组（前若干行说明 + 表头行 + 数据行）
export const WECHAT_ROWS = [
  ['微信支付账单明细'],
  ['微信昵称：[测试]'],
  ...Array(12).fill(['说明行']),
  ['交易时间','交易类型','交易对方','商品','收/支','金额(元)','支付方式','当前状态','交易单号','商户单号','备注'],
  [new Date('2026-05-31T12:48:45'),'商户消费','测试外卖','某餐厅订单','支出',19.6,'零钱','支付成功','WX0001','WM0001','/'],
  [new Date('2026-05-30T10:00:00'),'转账','某人','转账备注','收入',35,'/','已存入零钱','WX0002','/','/'],
  [new Date('2026-05-29T08:00:00'),'扫二维码付款','打印店','二维码收款','支出',4,'零钱','已转账','WX0003','WM0003','/'],
];
```

- [ ] **Step 2: 提交**

```bash
git add tests/fixtures.js
git commit -m "test: 合成账单 fixture（虚构数据，不含真实账单）"
```

---

### Task 4: 支付宝 CSV 解析器

**Files:**
- Create: `js/parse-alipay.js`
- Test: `tests/parse-alipay.test.js`

- [ ] **Step 1: 写失败测试（只测纯逻辑，不加载 PapaParse）**

```javascript
import { test } from 'node:test';
import assert from 'node:assert';
const { stripAlipayHeader, cleanRow } = await import('../js/parse-alipay.js');
import { ALIPAY_CSV_TEXT } from './fixtures.js';

test('定位表头行：丢弃前面的说明行，返回以表头开头的文本', () => {
  const body = stripAlipayHeader(ALIPAY_CSV_TEXT);
  assert.ok(body.startsWith('交易时间,交易分类'));
});

test('找不到表头时抛错', () => {
  assert.throws(() => stripAlipayHeader('没有表头的文本'), /未找到支付宝表头行/);
});

test('cleanRow 去除键名与值的空白、订单号尾部制表符', () => {
  const r = cleanRow({ '交易订单号': 'ALI0001\t', ' 金额 ': ' 21.92 ' });
  assert.strictEqual(r['交易订单号'], 'ALI0001');
  assert.strictEqual(r['金额'], '21.92');
});
```
> 说明：CSV 字段切分由 PapaParse 负责，是成熟库无需我们写单测；这里只测我们自己写的"定位表头 + 清洗行"纯逻辑。完整 Papa 解析（条数、含逗号字段）在 Task 8 用真实文件验收。

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/parse-alipay.test.js`
预期：FAIL（模块或函数不存在）。

- [ ] **Step 3: 实现 parse-alipay.js**

```javascript
// 依赖全局 Papa（浏览器由 vendor 脚本注入 window.Papa；Node 测试不调用 parseAlipayCsv，故不需要）

// 纯逻辑：定位表头行，返回从表头开始的 CSV 文本
export function stripAlipayHeader(text) {
  const lines = text.split(/\r?\n/);
  const headerIdx = lines.findIndex(l => l.trimStart().startsWith('交易时间'));
  if (headerIdx === -1) throw new Error('未找到支付宝表头行');
  return lines.slice(headerIdx).join('\n');
}

// 纯逻辑：清洗单行（键名/值去空白，订单号去尾部制表符）
export function cleanRow(r) {
  const out = {};
  for (const k of Object.keys(r)) out[k.trim()] = (r[k] ?? '').trim();
  return out;
}

// 集成：完整解析，使用全局 Papa（浏览器中运行）
export function parseAlipayCsv(text) {
  const body = stripAlipayHeader(text);
  const parsed = Papa.parse(body, { header: true, skipEmptyLines: true });
  return parsed.data
    .filter(r => (r['交易时间'] || '').trim())
    .map(cleanRow);
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/parse-alipay.test.js`
预期：PASS（3 通过）。

- [ ] **Step 5: 提交**

```bash
git add js/parse-alipay.js tests/parse-alipay.test.js
git commit -m "feat: 支付宝CSV解析器（定位表头行+按列名取值+订单号trim）"
```

---

### Task 5: 微信 xlsx 行解析器

**Files:**
- Create: `js/parse-wechat.js`
- Test: `tests/parse-wechat.test.js`

- [ ] **Step 1: 写失败测试（只测纯逻辑 parseWechatRows，不测文件读取）**

```javascript
import { test } from 'node:test';
import assert from 'node:assert';
const { parseWechatRows } = await import('../js/parse-wechat.js');
import { WECHAT_ROWS } from './fixtures.js';

test('定位微信表头行并解析数据', () => {
  const rows = parseWechatRows(WECHAT_ROWS);
  assert.strictEqual(rows.length, 3);
  assert.strictEqual(rows[0]['交易对方'], '测试外卖');
  assert.strictEqual(rows[0]['金额(元)'], 19.6);
  assert.strictEqual(rows[0]['收/支'], '支出');
});

test('交易时间保留为 Date 对象', () => {
  const rows = parseWechatRows(WECHAT_ROWS);
  assert.ok(rows[0]['交易时间'] instanceof Date);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/parse-wechat.test.js`
预期：FAIL。

- [ ] **Step 3: 实现 parse-wechat.js**

```javascript
// readWechatFile：浏览器中用 SheetJS 读取 File，cellDates 让时间序列号转 Date
export async function readWechatFile(file) {
  const buf = await file.arrayBuffer();
  const wb = XLSX.read(buf, { type: 'array', cellDates: true });
  const sheet = wb.Sheets[wb.SheetNames[0]];
  return XLSX.utils.sheet_to_json(sheet, { header: 1, raw: true, defval: '' });
}

// parseWechatRows：纯逻辑，输入二维数组，定位表头后映射为对象
export function parseWechatRows(rows) {
  const headerIdx = rows.findIndex(r => Array.isArray(r) && r[0] === '交易时间');
  if (headerIdx === -1) throw new Error('未找到微信表头行');
  const header = rows[headerIdx].map(h => String(h).trim());
  return rows.slice(headerIdx + 1)
    .filter(r => r && r[0] !== '' && r[0] != null)
    .map(r => {
      const out = {};
      header.forEach((h, i) => { out[h] = r[i] ?? ''; });
      return out;
    });
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/parse-wechat.test.js`
预期：PASS（2 通过）。

- [ ] **Step 5: 提交**

```bash
git add js/parse-wechat.js tests/parse-wechat.test.js
git commit -m "feat: 微信xlsx行解析器（定位表头+保留Date），文件读取走SheetJS cellDates"
```

---

### Task 6: GBK 解码器（浏览器实测）

**Files:**
- Create: `js/decode.js`

- [ ] **Step 1: 实现 decode.js**

```javascript
// 支付宝 CSV 为 GBK 编码；浏览器内置 TextDecoder 支持 'gbk'
export function decodeGbk(arrayBuffer) {
  return new TextDecoder('gbk').decode(new Uint8Array(arrayBuffer));
}

// 自动嗅探：UTF-8 解码若出现替换符(�)则回退 GBK
export function decodeSmart(arrayBuffer) {
  const utf8 = new TextDecoder('utf-8', { fatal: false }).decode(arrayBuffer);
  if (utf8.includes('�')) return new TextDecoder('gbk').decode(arrayBuffer);
  return utf8;
}
```

- [ ] **Step 2: 浏览器验收（用真实支付宝文件）**

操作：在临时调试页或 console 中读取真实支付宝 CSV，`decodeSmart(buffer)` 后打印前 30 行。
预期：中文正常（"交易时间""餐饮美食"），无乱码方块。
说明：解码不写 node 测试——GBK 编码 fixture 制作成本高，且真实文件验收更有意义。

- [ ] **Step 3: 提交**

```bash
git add js/decode.js
git commit -m "feat: GBK/UTF-8 智能解码（真实文件验收中文不乱码）"
```

---

### Task 7: 归一化为统一 Transaction

**Files:**
- Create: `js/normalize.js`
- Test: `tests/normalize.test.js`

- [ ] **Step 1: 写失败测试**

```javascript
import { test } from 'node:test';
import assert from 'node:assert';
const { normalizeAlipay, normalizeWechat } = await import('../js/normalize.js');

test('支付宝归一：方向/金额/来源/ISO时间', () => {
  const t = normalizeAlipay({
    '交易时间':'2026-05-30 20:17:08','交易分类':'餐饮美食','交易对方':'商户A',
    '商品说明':'午餐','收/支':'支出','金额':'21.92','收/付方式':'中国银行',
    '交易状态':'交易成功','交易订单号':'ALI0001','备注':''
  });
  assert.strictEqual(t.source, '支付宝');
  assert.strictEqual(t.amount, 21.92);
  assert.strictEqual(t.direction, '支出');
  assert.strictEqual(t.id, 'ALI0001');
  assert.strictEqual(t.datetime, '2026-05-30T20:17:08');
});

test('支付宝不计收支 -> 中性', () => {
  const t = normalizeAlipay({'交易时间':'2026-05-28 12:00:00','收/支':'不计收支',
    '金额':'500.00','交易订单号':'ALI0003','交易分类':'','交易对方':'','商品说明':'',
    '收/付方式':'','交易状态':'交易成功','备注':''});
  assert.strictEqual(t.direction, '中性');
});

test('微信归一：Date转ISO，中性交易->中性', () => {
  const t = normalizeWechat({
    '交易时间':new Date('2026-05-31T12:48:45'),'交易类型':'商户消费','交易对方':'外卖',
    '商品':'订单','收/支':'支出','金额(元)':19.6,'支付方式':'零钱',
    '当前状态':'支付成功','交易单号':'WX0001','备注':'/'
  });
  assert.strictEqual(t.source, '微信');
  assert.strictEqual(t.amount, 19.6);
  assert.strictEqual(t.datetime, '2026-05-31T12:48:45');
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/normalize.test.js`
预期：FAIL。

- [ ] **Step 3: 实现 normalize.js**

```javascript
function mapDirection(raw) {
  if (raw === '支出') return '支出';
  if (raw === '收入') return '收入';
  return '中性'; // 不计收支 / 中性交易
}

function toIso(v) {
  if (v instanceof Date) {
    const p = n => String(n).padStart(2, '0');
    return `${v.getFullYear()}-${p(v.getMonth()+1)}-${p(v.getDate())}` +
           `T${p(v.getHours())}:${p(v.getMinutes())}:${p(v.getSeconds())}`;
  }
  return String(v).trim().replace(' ', 'T');
}

export function normalizeAlipay(r) {
  return {
    id: String(r['交易订单号']).trim(),
    source: '支付宝',
    datetime: toIso(r['交易时间']),
    amount: parseFloat(r['金额']) || 0,
    direction: mapDirection(r['收/支']),
    counterparty: r['交易对方'] || '',
    description: r['商品说明'] || '',
    rawCategory: r['交易分类'] || '',
    myCategory: '',
    status: r['交易状态'] || '',
    payMethod: r['收/付方式'] || '',
    note: r['备注'] || ''
  };
}

export function normalizeWechat(r) {
  return {
    id: String(r['交易单号']).trim(),
    source: '微信',
    datetime: toIso(r['交易时间']),
    amount: parseFloat(r['金额(元)']) || 0,
    direction: mapDirection(r['收/支']),
    counterparty: r['交易对方'] || '',
    description: r['商品'] || '',
    rawCategory: r['交易类型'] || '',
    myCategory: '',
    status: r['当前状态'] || '',
    payMethod: r['支付方式'] || '',
    note: r['备注'] || ''
  };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/normalize.test.js`
预期：PASS（3 通过）。

- [ ] **Step 5: 提交**

```bash
git add js/normalize.js tests/normalize.test.js
git commit -m "feat: 归一化支付宝/微信记录为统一Transaction模型"
```

---

### Task 8: 导入流程接线 + 表格列出（浏览器验收）

**Files:**
- Modify: `index.html`（加入导入控件与表格容器）
- Modify: `js/app.js`（装配导入流程）
- Modify: `index.html`（在 app.js 前以普通 script 引入 vendor 库，暴露全局 Papa/XLSX）

- [ ] **Step 1: index.html 加入 vendor 脚本与导入 UI**

在 `</head>` 前加入（普通脚本，暴露全局 `Papa`/`XLSX`，供 ES Module 使用）：
```html
  <script src="js/vendor/papaparse.min.js"></script>
  <script src="js/vendor/xlsx.full.min.js"></script>
```
将 `<main>` 内容替换为：
```html
  <main id="app">
    <section class="card">
      <p>导入账单文件（支付宝 .csv / 微信 .xlsx）：</p>
      <input type="file" id="fileInput" accept=".csv,.xlsx" multiple>
      <p id="importMsg"></p>
    </section>
    <section class="card">
      <table id="txnTable"><thead><tr>
        <th>时间</th><th>来源</th><th>金额</th><th>方向</th><th>对方</th><th>说明</th>
      </tr></thead><tbody></tbody></table>
    </section>
  </main>
```

- [ ] **Step 2: style.css 追加卡片与表格样式**

```css
.card { background:#fff; border-radius:12px; padding:16px; margin-bottom:16px;
  box-shadow:0 1px 4px rgba(0,0,0,.06); }
table { width:100%; border-collapse:collapse; font-size:13px; }
th,td { padding:6px 4px; border-bottom:1px solid #eee; text-align:left; white-space:nowrap; }
td:nth-child(6){ white-space:normal; }
```

- [ ] **Step 3: 重写 js/app.js 接线导入→解析→归一→列出**

```javascript
import { decodeSmart } from './decode.js';
import { parseAlipayCsv } from './parse-alipay.js';
import { readWechatFile, parseWechatRows } from './parse-wechat.js';
import { normalizeAlipay, normalizeWechat } from './normalize.js';

let transactions = [];

async function fileToTransactions(file) {
  if (file.name.endsWith('.csv')) {
    const text = decodeSmart(await file.arrayBuffer());
    return parseAlipayCsv(text).map(normalizeAlipay);
  }
  if (file.name.endsWith('.xlsx')) {
    const rows = await readWechatFile(file);
    return parseWechatRows(rows).map(normalizeWechat);
  }
  throw new Error('不支持的文件类型：' + file.name);
}

function renderTable() {
  const tbody = document.querySelector('#txnTable tbody');
  tbody.innerHTML = transactions.map(t => `<tr>
    <td>${t.datetime.replace('T',' ')}</td><td>${t.source}</td>
    <td>${t.amount.toFixed(2)}</td><td>${t.direction}</td>
    <td>${t.counterparty}</td><td>${t.description}</td></tr>`).join('');
}

async function onFiles(e) {
  const msg = document.getElementById('importMsg');
  try {
    for (const f of e.target.files) {
      const txns = await fileToTransactions(f);
      transactions.push(...txns);
    }
    renderTable();
    msg.textContent = `已载入 ${transactions.length} 条记录`;
  } catch (err) { msg.textContent = '导入失败：' + err.message; }
}

document.getElementById('fileInput').addEventListener('change', onFiles);
```

- [ ] **Step 4: 浏览器验收（用两份真实文件）**

操作：本地服务器打开页面，分别选择真实支付宝 CSV 与微信 xlsx。
预期：
- 中文不乱码、时间显示正常（非数字序列号）。
- 载入条数与文件头部统计的"总记录数"量级一致（注意：此时尚未去重/过滤，条数应≈各文件原始行数之和）。

- [ ] **Step 5: 提交**

```bash
git add index.html css/style.css js/app.js
git commit -m "feat: 导入流程接线，解析两类账单并列表展示（真实文件验收）"
```

---

## 阶段 2：持久化与去重

### Task 9: 去重与"计入消费"判定

**Files:**
- Create: `js/dedup.js`
- Test: `tests/dedup.test.js`

- [ ] **Step 1: 写失败测试**

```javascript
import { test } from 'node:test';
import assert from 'node:assert';
const { mergeTransactions, isCountableExpense } = await import('../js/dedup.js');

const mk = (id, dir='支出', status='交易成功', amount=10) =>
  ({ id, direction:dir, status, amount, source:'支付宝' });

test('按 id 去重，返回新增与跳过计数', () => {
  const existing = [mk('A'), mk('B')];
  const incoming = [mk('B'), mk('C')]; // B 重复
  const { merged, added, skipped } = mergeTransactions(existing, incoming);
  assert.strictEqual(merged.length, 3);
  assert.strictEqual(added, 1);
  assert.strictEqual(skipped, 1);
});

test('计入消费：仅支出且非交易关闭', () => {
  assert.strictEqual(isCountableExpense(mk('X','支出','交易成功')), true);
  assert.strictEqual(isCountableExpense(mk('X','支出','交易关闭')), false);
  assert.strictEqual(isCountableExpense(mk('X','中性','交易成功')), false);
  assert.strictEqual(isCountableExpense(mk('X','收入','交易成功')), false);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/dedup.test.js`
预期：FAIL。

- [ ] **Step 3: 实现 dedup.js**

```javascript
export function mergeTransactions(existing, incoming) {
  const seen = new Set(existing.map(t => t.id));
  let added = 0, skipped = 0;
  const merged = existing.slice();
  for (const t of incoming) {
    if (!t.id) { skipped++; continue; }     // 无订单号的记录跳过
    if (seen.has(t.id)) { skipped++; continue; }
    seen.add(t.id); merged.push(t); added++;
  }
  return { merged, added, skipped };
}

export function isCountableExpense(t) {
  return t.direction === '支出' && t.status !== '交易关闭';
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/dedup.test.js`
预期：PASS（2 通过）。

- [ ] **Step 5: 提交**

```bash
git add js/dedup.js tests/dedup.test.js
git commit -m "feat: 订单号去重 + 计入消费判定（排除中性/收入/交易关闭）"
```

---

### Task 10: IndexedDB 持久化

**Files:**
- Create: `js/storage.js`
- Modify: `js/app.js`（导入后存库、启动时载入）

- [ ] **Step 1: 实现 storage.js**

```javascript
const DB_NAME = 'jizhang', STORE_TXN = 'transactions', STORE_MEM = 'merchantMemory';

function openDb() {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, 1);
    req.onupgradeneeded = () => {
      const db = req.result;
      if (!db.objectStoreNames.contains(STORE_TXN))
        db.createObjectStore(STORE_TXN, { keyPath: 'id' });
      if (!db.objectStoreNames.contains(STORE_MEM))
        db.createObjectStore(STORE_MEM, { keyPath: 'counterparty' });
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}

export async function saveTransactions(txns) {
  const db = await openDb();
  const tx = db.transaction(STORE_TXN, 'readwrite');
  const store = tx.objectStore(STORE_TXN);
  for (const t of txns) store.put(t);
  return new Promise((res, rej) => { tx.oncomplete = res; tx.onerror = () => rej(tx.error); });
}

export async function loadAllTransactions() {
  const db = await openDb();
  return new Promise((res, rej) => {
    const req = db.transaction(STORE_TXN).objectStore(STORE_TXN).getAll();
    req.onsuccess = () => res(req.result); req.onerror = () => rej(req.error);
  });
}

export async function saveMemory(counterparty, category) {
  const db = await openDb();
  const tx = db.transaction(STORE_MEM, 'readwrite');
  tx.objectStore(STORE_MEM).put({ counterparty, category });
  return new Promise((res, rej) => { tx.oncomplete = res; tx.onerror = () => rej(tx.error); });
}

export async function loadMemory() {
  const db = await openDb();
  return new Promise((res, rej) => {
    const req = db.transaction(STORE_MEM).objectStore(STORE_MEM).getAll();
    req.onsuccess = () => {
      const map = {};
      for (const row of req.result) map[row.counterparty] = row.category;
      res(map);
    };
    req.onerror = () => rej(req.error);
  });
}
```

- [ ] **Step 2: app.js 接入存库与启动载入**

将 app.js 顶部 import 增加：
```javascript
import { mergeTransactions } from './dedup.js';
import { saveTransactions, loadAllTransactions } from './storage.js';
```
把 `onFiles` 改为去重后入库：
```javascript
async function onFiles(e) {
  const msg = document.getElementById('importMsg');
  try {
    let incoming = [];
    for (const f of e.target.files) incoming.push(...await fileToTransactions(f));
    const { merged, added, skipped } = mergeTransactions(transactions, incoming);
    transactions = merged;
    await saveTransactions(incoming);   // put 幂等，重复 id 覆盖不增量
    renderTable();
    msg.textContent = `本次新增 ${added} 条，去重跳过 ${skipped} 条，共 ${transactions.length} 条`;
  } catch (err) { msg.textContent = '导入失败：' + err.message; }
}
```
在文件末尾加启动载入：
```javascript
(async () => { transactions = await loadAllTransactions(); renderTable(); })();
```

- [ ] **Step 3: 浏览器验收（去重 + 持久化）**

操作：
1. 导入真实文件 → 记下"共 N 条"。
2. **再次导入同一文件** → 应显示"新增 0 条，去重跳过 M 条"，总数不变。
3. 刷新页面 → 表格仍有数据（已持久化）。
预期：账目不翻倍；刷新不丢。

- [ ] **Step 4: 提交**

```bash
git add js/storage.js js/app.js
git commit -m "feat: IndexedDB 持久化 + 导入去重，刷新不丢、重复导入不翻倍"
```

---

## 阶段 3：分类与手动归类

### Task 11: 分类引擎

**Files:**
- Create: `js/classify.js`
- Test: `tests/classify.test.js`

- [ ] **Step 1: 写失败测试**

```javascript
import { test } from 'node:test';
import assert from 'node:assert';
const { classify, CATEGORIES } = await import('../js/classify.js');

const base = { counterparty:'', description:'', rawCategory:'', source:'支付宝', direction:'支出' };

test('商户记忆优先级最高', () => {
  const t = { ...base, counterparty:'神秘小店', rawCategory:'餐饮美食' };
  assert.strictEqual(classify(t, { '神秘小店':'学习教育' }), '学习教育');
});

test('支付宝原始分类映射', () => {
  assert.strictEqual(classify({ ...base, rawCategory:'餐饮美食' }, {}), '餐饮');
  assert.strictEqual(classify({ ...base, rawCategory:'交通出行' }, {}), '交通');
});

test('关键词规则（微信无好分类时）', () => {
  const t = { ...base, source:'微信', counterparty:'美团', rawCategory:'商户消费' };
  assert.strictEqual(classify(t, {}), '餐饮');
});

test('地铁关键词 -> 交通', () => {
  const t = { ...base, source:'微信', counterparty:'北京轨道交通', rawCategory:'扫二维码付款' };
  assert.strictEqual(classify(t, {}), '交通');
});

test('都未命中 -> 其他', () => {
  assert.strictEqual(classify({ ...base, counterparty:'xyz', description:'zzz' }, {}), '其他');
});

test('CATEGORIES 含学生版分类', () => {
  assert.ok(CATEGORIES.includes('学习教育'));
  assert.ok(CATEGORIES.includes('人情往来'));
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/classify.test.js`
预期：FAIL。

- [ ] **Step 3: 实现 classify.js**

```javascript
export const CATEGORIES = ['餐饮','交通','购物','学习教育','娱乐休闲',
  '话费通讯','生活缴费','医疗健康','人情往来','其他'];

// 支付宝原始"交易分类" -> 统一分类
const ALIPAY_CATEGORY_MAP = {
  '餐饮美食':'餐饮','交通出行':'交通','日用百货':'购物','服饰装扮':'购物',
  '数码电器':'购物','文化休闲':'娱乐休闲','教育培训':'学习教育',
  '充值缴费':'生活缴费','医疗健康':'医疗健康','美容美发':'购物','母婴亲子':'购物',
  '生活服务':'生活缴费','商业服务':'其他','住房物业':'生活缴费',
  '运动户外':'娱乐休闲','酒店旅游':'娱乐休闲','转账红包':'人情往来'
};

// 关键词规则（命中 counterparty 或 description 即归类），顺序优先
const KEYWORD_RULES = [
  ['餐饮', ['美团','饿了么','瑞幸','蜜雪','星巴克','餐','外卖','奶茶','咖啡','食堂','乡村基']],
  ['交通', ['地铁','公交','轨道交通','高德','滴滴','打车','单车','火车','12306','出行']],
  ['学习教育', ['打印','文印','图书','书店','文具','培训','教育','学费','考试','报名']],
  ['娱乐休闲', ['Steam','Valve','游戏','会员','视频','音乐','电影','网吧']],
  ['购物', ['淘宝','天猫','京东','拼多多','超市','便利店','服饰','旗舰店']],
  ['话费通讯', ['话费','流量','移动','联通','电信','宽带']],
  ['生活缴费', ['校园卡','一卡通','充值','水费','电费','宿舍']],
  ['人情往来', ['转账','红包']],
];

export function classify(t, memory = {}) {
  // 1. 商户记忆优先
  if (t.counterparty && memory[t.counterparty]) return memory[t.counterparty];
  // 2. 支付宝原始分类映射
  if (t.source === '支付宝' && ALIPAY_CATEGORY_MAP[t.rawCategory])
    return ALIPAY_CATEGORY_MAP[t.rawCategory];
  // 3. 关键词规则
  const hay = `${t.counterparty} ${t.description}`;
  for (const [cat, words] of KEYWORD_RULES)
    if (words.some(w => hay.includes(w))) return cat;
  // 4. 兜底
  return '其他';
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/classify.test.js`
预期：PASS（6 通过）。

- [ ] **Step 5: 提交**

```bash
git add js/classify.js tests/classify.test.js
git commit -m "feat: 分类引擎（商户记忆>支付宝映射>关键词>其他），学生版分类体系"
```

---

### Task 12: 分类接入导入流程 + 明细页手动改类

**Files:**
- Modify: `js/app.js`（导入时分类、表格加分类列与下拉、改类写记忆）

- [ ] **Step 1: app.js 接入分类**

顶部 import 增加：
```javascript
import { classify, CATEGORIES } from './classify.js';
import { saveMemory, loadMemory } from './storage.js';
```
增加内存中的记忆表，并在归一后分类：
```javascript
let memory = {};
function classifyAll() { for (const t of transactions) t.myCategory = classify(t, memory); }
```
在 `onFiles` 里 `transactions = merged;` 之后、`renderTable()` 之前加 `classifyAll();`，并在入库改为存分类后的全集：
```javascript
    classifyAll();
    await saveTransactions(transactions);
```
启动载入改为：
```javascript
(async () => {
  memory = await loadMemory();
  transactions = await loadAllTransactions();
  classifyAll();
  renderTable();
})();
```

- [ ] **Step 2: 表格加"分类"列与下拉选择，改类即写记忆**

`renderTable` 表头加 `<th>分类</th>`，并改为带下拉：
```javascript
function renderTable() {
  const tbody = document.querySelector('#txnTable tbody');
  tbody.innerHTML = transactions.map((t, i) => {
    const opts = CATEGORIES.map(c =>
      `<option value="${c}"${c===t.myCategory?' selected':''}>${c}</option>`).join('');
    const hl = t.myCategory === '其他' ? ' class="hl"' : '';
    return `<tr${hl}>
      <td>${t.datetime.replace('T',' ')}</td><td>${t.source}</td>
      <td>${t.amount.toFixed(2)}</td><td>${t.direction}</td>
      <td>${t.counterparty}</td><td>${t.description}</td>
      <td><select data-i="${i}">${opts}</select></td></tr>`;
  }).join('');
  tbody.querySelectorAll('select').forEach(sel =>
    sel.addEventListener('change', onChangeCategory));
}

async function onChangeCategory(e) {
  const i = +e.target.dataset.i, cat = e.target.value;
  const t = transactions[i];
  t.myCategory = cat;
  memory[t.counterparty] = cat;            // 记住该商户
  await saveMemory(t.counterparty, cat);
  await saveTransactions([t]);
  // 同商户的其它记录一并更新
  for (const o of transactions)
    if (o.counterparty === t.counterparty) o.myCategory = cat;
  renderTable();
}
```

- [ ] **Step 3: style.css 追加未分类高亮**

```css
tr.hl { background: #fff7e6; }
select { font-size: 13px; padding: 2px; }
```

- [ ] **Step 4: 浏览器验收（分类 + 记忆）**

操作：
1. 导入真实文件 → 大部分记录自动归类，"其他"行高亮。
2. 把某条"其他"改成某类 → 该商户**所有**记录立即跟着变。
3. 刷新页面 → 分类保持；再次导入含该商户的新数据 → 自动归到记住的类。
预期：手动改一次即被记住并复用。

- [ ] **Step 5: 提交**

```bash
git add js/app.js css/style.css
git commit -m "feat: 导入自动分类 + 明细下拉改类并记忆商户，未分类高亮"
```

---

## 阶段 4：仪表盘

### Task 13: 统计聚合

**Files:**
- Create: `js/aggregate.js`
- Test: `tests/aggregate.test.js`

- [ ] **Step 1: 写失败测试**

```javascript
import { test } from 'node:test';
import assert from 'node:assert';
const { summarize } = await import('../js/aggregate.js');

const txns = [
  { datetime:'2026-05-01T10:00:00', amount:20, direction:'支出', status:'交易成功', myCategory:'餐饮' },
  { datetime:'2026-05-01T12:00:00', amount:5,  direction:'支出', status:'交易成功', myCategory:'交通' },
  { datetime:'2026-05-02T09:00:00', amount:30, direction:'支出', status:'交易成功', myCategory:'餐饮' },
  { datetime:'2026-05-02T09:30:00', amount:99, direction:'支出', status:'交易关闭', myCategory:'购物' }, // 废单不计
  { datetime:'2026-05-02T10:00:00', amount:500,direction:'中性', status:'交易成功', myCategory:'其他' }, // 中性不计
  { datetime:'2026-05-03T10:00:00', amount:200,direction:'收入', status:'交易成功', myCategory:'其他' },
];

test('总支出只计可计入消费', () => {
  const s = summarize(txns, { start:'2026-05-01', end:'2026-05-31' });
  assert.strictEqual(s.totalExpense, 55);   // 20+5+30
  assert.strictEqual(s.totalIncome, 200);
});

test('分类汇总', () => {
  const s = summarize(txns, { start:'2026-05-01', end:'2026-05-31' });
  assert.strictEqual(s.byCategory['餐饮'], 50);
  assert.strictEqual(s.byCategory['交通'], 5);
  assert.strictEqual(s.byCategory['购物'], undefined); // 废单未计
});

test('按日汇总', () => {
  const s = summarize(txns, { start:'2026-05-01', end:'2026-05-31' });
  assert.strictEqual(s.byDay['2026-05-01'], 25);
  assert.strictEqual(s.byDay['2026-05-02'], 30);
});

test('时间范围过滤', () => {
  const s = summarize(txns, { start:'2026-05-02', end:'2026-05-02' });
  assert.strictEqual(s.totalExpense, 30);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test tests/aggregate.test.js`
预期：FAIL。

- [ ] **Step 3: 实现 aggregate.js**

```javascript
import { isCountableExpense } from './dedup.js';

export function summarize(transactions, range) {
  const inRange = t => {
    const d = t.datetime.slice(0, 10);
    return (!range?.start || d >= range.start) && (!range?.end || d <= range.end);
  };
  const s = { totalExpense: 0, totalIncome: 0, net: 0, byCategory: {}, byDay: {} };
  for (const t of transactions) {
    if (!inRange(t)) continue;
    if (isCountableExpense(t)) {
      s.totalExpense += t.amount;
      const cat = t.myCategory || '其他';
      s.byCategory[cat] = (s.byCategory[cat] || 0) + t.amount;
      const day = t.datetime.slice(0, 10);
      s.byDay[day] = (s.byDay[day] || 0) + t.amount;
    } else if (t.direction === '收入') {
      s.totalIncome += t.amount;
    }
  }
  // 浮点修正
  s.totalExpense = Math.round(s.totalExpense * 100) / 100;
  s.totalIncome = Math.round(s.totalIncome * 100) / 100;
  s.net = Math.round((s.totalIncome - s.totalExpense) * 100) / 100;
  return s;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test tests/aggregate.test.js`
预期：PASS（4 通过）。

- [ ] **Step 5: 提交**

```bash
git add js/aggregate.js tests/aggregate.test.js
git commit -m "feat: 统计聚合（总支出/收入/分类占比/按日，含范围过滤与口径）"
```

---

### Task 14: 图表渲染

**Files:**
- Create: `js/charts.js`
- Modify: `index.html`（引入 Chart.js vendor + 仪表盘容器）

- [ ] **Step 1: index.html 引入 Chart.js 并加仪表盘区**

`</head>` 前加：`<script src="js/vendor/chart.umd.min.js"></script>`
在导入卡片之后、表格之前插入仪表盘：
```html
    <section class="card" id="dashboard">
      <div class="range">
        <label>月份 <input type="month" id="monthPick"></label>
        <button id="allRange">全部</button>
      </div>
      <div class="totals">
        <span>支出 <b id="sumExpense">0</b></span>
        <span>收入 <b id="sumIncome">0</b></span>
        <span>净额 <b id="sumNet">0</b></span>
      </div>
      <canvas id="pieChart" height="220"></canvas>
      <canvas id="trendChart" height="200"></canvas>
    </section>
```

- [ ] **Step 2: 实现 charts.js**

```javascript
let pie, trend;

export function renderCategoryPie(canvas, byCategory) {
  const labels = Object.keys(byCategory);
  const data = labels.map(k => byCategory[k]);
  if (pie) pie.destroy();
  pie = new Chart(canvas, {
    type: 'doughnut',
    data: { labels, datasets: [{ data,
      backgroundColor: ['#2c7be5','#e63757','#00d97e','#f6c343','#39afd1',
        '#6b5eae','#fd7e14','#95aac9','#a6d96a','#d2d6de'] }] },
    options: { plugins: { legend: { position: 'right' } } }
  });
}

export function renderTrend(canvas, byDay) {
  const labels = Object.keys(byDay).sort();
  const data = labels.map(k => byDay[k]);
  if (trend) trend.destroy();
  trend = new Chart(canvas, {
    type: 'bar',
    data: { labels, datasets: [{ label: '每日支出', data, backgroundColor: '#2c7be5' }] },
    options: { plugins: { legend: { display: false } },
      scales: { y: { beginAtZero: true } } }
  });
}
```

- [ ] **Step 3: style.css 追加仪表盘样式**

```css
.totals { display:flex; gap:16px; margin:12px 0; flex-wrap:wrap; }
.totals span { font-size:14px; } .totals b { font-size:18px; color:#2c7be5; }
.range { display:flex; gap:12px; align-items:center; }
canvas { margin-top:12px; }
```

- [ ] **Step 4: 提交**

```bash
git add index.html js/charts.js css/style.css
git commit -m "feat: Chart.js 分类饼图 + 每日支出柱状图"
```

---

### Task 15: 仪表盘接线（月份切换 + 刷新）

**Files:**
- Modify: `js/app.js`（计算汇总、渲染图表与总额、月份联动）

- [ ] **Step 1: app.js 接入仪表盘**

顶部 import 增加：
```javascript
import { summarize } from './aggregate.js';
import { renderCategoryPie, renderTrend } from './charts.js';
```
增加当前范围状态与刷新函数：
```javascript
let range = currentMonthRange();
function currentMonthRange() {
  const now = new Date();
  const ym = `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}`;
  return { start: `${ym}-01`, end: `${ym}-31`, label: ym };
}

function refreshDashboard() {
  const s = summarize(transactions, range);
  document.getElementById('sumExpense').textContent = s.totalExpense.toFixed(2);
  document.getElementById('sumIncome').textContent = s.totalIncome.toFixed(2);
  document.getElementById('sumNet').textContent = s.net.toFixed(2);
  renderCategoryPie(document.getElementById('pieChart'), s.byCategory);
  renderTrend(document.getElementById('trendChart'), s.byDay);
}
```
月份与"全部"按钮联动：
```javascript
document.getElementById('monthPick').addEventListener('change', e => {
  const ym = e.target.value;            // "2026-05"
  if (!ym) return;
  range = { start: `${ym}-01`, end: `${ym}-31`, label: ym };
  refreshDashboard();
});
document.getElementById('allRange').addEventListener('click', () => {
  range = { start: null, end: null, label: '全部' };
  refreshDashboard();
});
```

- [ ] **Step 2: 在所有数据变化处调用 refreshDashboard**

在 `onFiles`、`onChangeCategory`、启动载入的 IIFE 中，于 `renderTable()` 后各加一行 `refreshDashboard();`。
并在启动 IIFE 里设置月份选择器默认值：
```javascript
  document.getElementById('monthPick').value = range.label;
```

- [ ] **Step 3: 浏览器验收（完整数据流）**

操作：导入两份真实文件后查看仪表盘。
预期：
- 总支出/收入/净额显示合理数字。
- 饼图能一眼看出各分类占比；柱状图显示每日支出。
- 切换月份，数字与图表随之变化；"全部"显示累计。
- 改某条分类，饼图占比即时更新。

- [ ] **Step 4: 提交**

```bash
git add js/app.js
git commit -m "feat: 仪表盘接线（总额+饼图+趋势），月份切换与数据变更实时刷新"
```

---

### Task 16: 数据备份导出/导入（防丢兜底）

**Files:**
- Modify: `js/storage.js`（导出/导入 JSON）
- Modify: `index.html`（备份按钮）
- Modify: `js/app.js`（按钮接线）

- [ ] **Step 1: storage.js 增加备份函数**

```javascript
export async function exportBackup() {
  const txns = await loadAllTransactions();
  const mem = await loadMemory();
  const blob = new Blob([JSON.stringify({ txns, mem }, null, 2)],
    { type: 'application/json' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url; a.download = `记账备份-${new Date().toISOString().slice(0,10)}.json`;
  a.click(); URL.revokeObjectURL(url);
}

export async function importBackup(file) {
  const { txns, mem } = JSON.parse(await file.text());
  if (txns) await saveTransactions(txns);
  if (mem) for (const [cp, cat] of Object.entries(mem)) await saveMemory(cp, cat);
}
```

- [ ] **Step 2: index.html 导入卡片内加备份控件**

```html
      <div class="backup">
        <button id="exportBtn">导出备份</button>
        <label class="btn-like">导入备份<input type="file" id="importBackup" accept=".json" hidden></label>
      </div>
```

- [ ] **Step 3: app.js 接线**

```javascript
import { exportBackup, importBackup } from './storage.js';
document.getElementById('exportBtn').addEventListener('click', exportBackup);
document.getElementById('importBackup').addEventListener('change', async e => {
  await importBackup(e.target.files[0]);
  memory = await loadMemory();
  transactions = await loadAllTransactions();
  classifyAll(); renderTable(); refreshDashboard();
});
```

- [ ] **Step 4: 浏览器验收**

操作：导出备份得到 JSON；清空浏览器数据后导入该 JSON。
预期：数据完整恢复（交易 + 商户记忆）。

- [ ] **Step 5: 提交**

```bash
git add js/storage.js index.html js/app.js
git commit -m "feat: JSON 备份导出/导入，防止清缓存或换设备丢数据"
```

---

## 阶段 5：PWA 装成手机 App

### Task 17: manifest + 图标

**Files:**
- Create: `manifest.webmanifest`
- Create: `icons/icon-192.png`, `icons/icon-512.png`
- Modify: `index.html`（引用 manifest）

- [ ] **Step 1: 创建 manifest.webmanifest**

```json
{
  "name": "我的记账",
  "short_name": "记账",
  "start_url": "./index.html",
  "display": "standalone",
  "background_color": "#f5f6f8",
  "theme_color": "#2c7be5",
  "icons": [
    { "src": "icons/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "icons/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **Step 2: 生成纯色占位图标（无需设计工具）**

```bash
mkdir -p icons
# 用 ImageMagick；若无则任取两张 192/512 正方形 png 命名放入 icons/
convert -size 192x192 xc:'#2c7be5' -gravity center -pointsize 90 -fill white -annotate 0 '记' icons/icon-192.png
convert -size 512x512 xc:'#2c7be5' -gravity center -pointsize 240 -fill white -annotate 0 '记' icons/icon-512.png
```
（若开发机无 convert：手动放入任意两张对应尺寸 PNG 即可，图标不影响功能。）

- [ ] **Step 3: index.html head 引用 manifest 与主题色**

```html
  <link rel="manifest" href="manifest.webmanifest">
  <meta name="theme-color" content="#2c7be5">
  <link rel="apple-touch-icon" href="icons/icon-192.png">
```

- [ ] **Step 4: 提交**

```bash
git add manifest.webmanifest icons/ index.html
git commit -m "feat: PWA manifest 与应用图标"
```

---

### Task 18: service worker 离线缓存

**Files:**
- Create: `sw.js`
- Modify: `js/app.js`（注册 sw）

- [ ] **Step 1: 创建 sw.js（缓存全部静态资源）**

```javascript
const CACHE = 'jizhang-v1';
const ASSETS = [
  './', './index.html', './css/style.css',
  './js/app.js','./js/decode.js','./js/parse-alipay.js','./js/parse-wechat.js',
  './js/normalize.js','./js/dedup.js','./js/classify.js','./js/aggregate.js',
  './js/storage.js','./js/charts.js',
  './js/vendor/papaparse.min.js','./js/vendor/xlsx.full.min.js','./js/vendor/chart.umd.min.js',
  './manifest.webmanifest','./icons/icon-192.png','./icons/icon-512.png'
];
self.addEventListener('install', e =>
  e.waitUntil(caches.open(CACHE).then(c => c.addAll(ASSETS))));
self.addEventListener('activate', e =>
  e.waitUntil(caches.keys().then(keys =>
    Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k))))));
self.addEventListener('fetch', e =>
  e.respondWith(caches.match(e.request).then(r => r || fetch(e.request))));
```

- [ ] **Step 2: app.js 末尾注册 service worker**

```javascript
if ('serviceWorker' in navigator) {
  window.addEventListener('load', () =>
    navigator.serviceWorker.register('./sw.js').catch(console.error));
}
```

- [ ] **Step 3: 浏览器验收（离线）**

操作：本地服务器加载一次后，断网（或 DevTools→Network→Offline）刷新。
预期：App 仍能打开并显示已存数据。DevTools→Application→Manifest 无报错、可"安装"。

- [ ] **Step 4: 提交**

```bash
git add sw.js js/app.js
git commit -m "feat: service worker 离线缓存，断网可用、可安装到主屏"
```

---

### Task 19: 部署上线 + 手机安装

**Files:**（无代码改动，部署与文档）
- Create: `README.md`（使用说明：导出账单步骤 + 每月使用流程）

- [ ] **Step 1: 部署到 Netlify Drop 或 GitHub Pages**

方式 A（最简单）：浏览器打开 https://app.netlify.com/drop ，把整个 `app/` 文件夹拖进去，得到 HTTPS 网址。
方式 B：推送到 GitHub 仓库 → Settings→Pages→部署分支根目录。
**部署前再次确认** `git ls-files` 中**无任何 csv/xlsx 账单文件**（隐私红线）。

- [ ] **Step 2: 手机安装**

手机浏览器（推荐 Chrome/Safari）打开网址 → 菜单"添加到主屏幕" → 桌面出现"记账"图标。

- [ ] **Step 3: 编写 README.md**

内容包含：
- 如何从支付宝导出 CSV（我-账单-右上角-开具交易流水证明/导出）。
- 如何从微信导出账单 xlsx（我-服务-钱包-账单-常见问题-下载账单-用于个人对账，邮箱收加密 zip，用短信收到的密码解压得 xlsx）。
- 每月使用流程：导出两份 → 打开 App → 各导入一次 → 看仪表盘 → 给"其他"高亮项归类。
- 隐私说明：数据全程本地、备份按钮的作用。

- [ ] **Step 4: 端到端验收（真实月度流程）**

操作：在手机安装后的 App 里完整走一遍：导入支付宝 CSV + 微信 xlsx → 查看本月仪表盘 → 归类若干"其他" → 导出备份。
预期：全流程顺畅，结果正确，符合第 10 节成功标准。

- [ ] **Step 5: 提交**

```bash
git add README.md
git commit -m "docs: 使用说明（账单导出步骤 + 每月使用流程 + 隐私说明）"
```

---

## 自检对照（Spec 覆盖）

- 形态 PWA / 技术栈 → Task 1,2,17,18,19 ✓
- 支付宝 GBK 解码 + 表头定位 → Task 4,6 ✓
- 微信 xlsx + Excel 日期序列号 → Task 5 ✓
- 统一数据模型 → Task 7 ✓
- 去重（订单号）+ 过滤中性/废单 → Task 9,13 ✓
- 退款口径（5.4 修正版：自动抵消，不额外扣减）→ Task 13 测试已覆盖"交易关闭/中性不计" ✓
- 学生版分类 + 商户记忆 → Task 11,12 ✓
- 仪表盘饼图/趋势/月份 → Task 14,15 ✓
- 备份防丢 → Task 16 ✓
- 隐私红线（账单不进库）→ .gitignore（已提交）+ Task 19 部署前复查 ✓

成功标准（Spec 第 10 节）由 Task 8/10/12/15 的浏览器验收 + Task 19 端到端验收共同保证。
