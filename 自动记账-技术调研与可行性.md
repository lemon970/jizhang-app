# 安卓自动记账 App — 技术调研与可行性

> 目标：做一个安卓端、后台自动识别交易、尽量不用手动逐条输入的个人记账 App。
> 本文档为前期调研存档，不是设计方案。日期：2026-05-31

---

## 一、能"自动拿到交易数据"的几种机制

安卓上没有官方的"读取微信/支付宝交易"的接口（个人收款回调只对企业资质开放），
所以所有自动记账都是绕过去拿数据。主流有 5 条路线：

| 路线 | 原理 | 能拿到什么 | 难度 | 可靠性 | 是否要 root |
|---|---|---|---|---|---|
| **无障碍服务 AccessibilityService** | 读取当前屏幕的控件节点树 | 金额、对方、账户、商品（支付成功页/账单详情页可见的全部文字） | 中 | 中（依赖页面规则） | 否 |
| **通知监听 NotificationListenerService** | 监听通知栏 title/content | 金额（部分通知有，部分没有） | 低 | 中低 | 否 |
| **读短信 READ_SMS** | 读银行交易短信 | 银行卡消费/到账（金额、余额、商户） | 低 | 中 | 否 |
| **Xposed / LSPosed Hook** | 直接 hook 微信支付宝进程内的数据 | 最完整、最准确的交易数据 | 高 | 高 | 需 root/解锁 |
| **账单文件导入** | 解析支付宝/微信导出的 CSV 账单 | 全量历史账单 | 低 | 高 | 否 |

补充机制：
- **UsageStatsManager**：只能知道"前台是哪个 App"，拿不到交易内容，仅用于辅助触发。
- **Shizuku**：免 root 提权方案，可给无障碍/部分能力增强，介于无障碍和 Xposed 之间。

---

## 二、各路线的真实坑（实测/社区反馈）

### 无障碍服务（钱迹、小星记账走的就是这条）
- 能识别的场景：支付宝/微信的扫码支付、付款码、转账、账单详情、零钱明细等**有金额显示的页面**。
- 坑 1 **保活**：华为/荣耀/小米/OPPO/vivo 的省电策略会杀后台进程，服务一被杀就失效。
  需引导用户开"自启动 + 忽略电池优化 + 后台锁定"，仍无法 100% 保证存活。
- 坑 2 **页面规则易碎**：微信/支付宝一更新 UI，识别规则就可能失效，需要持续适配。
  钱迹官方原话："微信最近几个版本调整了无障碍逻辑，导致支付完成后无法弹窗，暂无彻底解决办法。"
- 坑 3 **只认中文**：界面是英文/繁体则识别不了。
- 坑 4 **鸿蒙不支持**：HarmonyOS 的无障碍不像 Android 那样公开，这条路在纯鸿蒙上走不通。
- 坑 5 **碰一碰等动画页面**：金额是动画渐显的，可能抓取不全。

### 通知监听
- 实现简单，权限申请友好（一个开关授权即可）。
- 坑：很多支付**通知里不带金额**，甚至不产生通知（如淘宝支付后）；信息少，
  通常只有金额、没有对方/分类。Android 15+ 还限制读取 OTP 类通知。
- 进程被杀后再启动时服务不生效，需用"disable→enable 组件"的技巧触发系统重新 bind。

### 读短信
- 适合**银行卡**交易（很多人微信支付绑的就是银行卡，会有扣款短信）。
- Google Play 严格限制（必须是默认短信 App 才能申请）——但**个人 sideload 不上架，则不受此限**。
- Android 17 起，含验证码(OTP)的短信会被延迟 3 小时投递；普通交易短信不受影响。

### Xposed / LSPosed
- 数据最准最全，不依赖屏幕页面。开源项目 `AutoAccountingOrg/AutoAccounting` 就是这条路。
- 代价：需要 root 或解锁 BL 刷 LSPosed 框架，门槛高、有风险、可能影响银行类 App 风控。

---

## 三、可参考的开源项目

| 项目 | 路线 | 说明 |
|---|---|---|
| `AutoAccountingOrg/AutoAccounting` | Xposed + 本地规则 + AI | 最成熟的自动记账模块 |
| `Auto-Accounting/AutoAccounting` | Xposed/无障碍/Shizuku | 多方案融合 |
| 钱迹（闭源，可参考交互） | 无障碍 | 行业标杆，弹窗确认式记账 |
| `QianjiAutoRecord`（gitee） | Auto.js 脚本 + 无障碍 | 脚本化、悬浮窗自动填充 |
| `BeeCount`（TNT-Likely） | 跨端 + AI 记账 | Flutter，含自建同步，离线优先 |
| `IceFake/FunnyExpenseTracking-Android` | 纯本地记账 | MVVM + Clean Architecture 参考 |
| `hnkjdaxzzq/autocoin` | Web + LLM 识别截图/账单 | 多模态 AI 入账思路 |

---

## 四、App 本体（存储 + UI）推荐技术栈

社区主流的本地记账 App 选型，也是最适合学习的一套：

| 类别 | 选型 |
|---|---|
| 语言 | Kotlin |
| UI | Jetpack Compose + Material3 |
| 架构 | 单 Activity + ViewModel + Repository（MVVM） |
| 本地存储 | Room（SQLite 抽象层，编译期校验 SQL） |
| 异步 | Kotlin Coroutines + Flow |
| SDK | minSdk 24 起，targetSdk 最新 |

数据流：`Compose UI → ViewModel → Repository → DAO + Room`

---

## 五、可行性结论

**结论：个人自用（不上架 Google Play）的前提下，可行——但要先放下"完全零干预、100% 准确"的预期。**

可以做到的：
- 后台监听，在你支付后**自动抓到大部分交易的金额**，弹窗一键确认或直接入账。
- 银行卡交易用短信兜底，微信/支付宝钱包交易用无障碍/通知抓取。
- 全程数据本地处理，不上传，隐私可控。

做不到 / 要妥协的：
- **后台保活无法 100%**：国产 ROM 会杀进程，偶尔需要手动重新唤起。这是安卓系统机制决定的，钱迹也解决不了。
- **页面规则需要持续维护**：微信/支付宝改版后识别可能失效。
- **覆盖面有限**：只对你写了规则的 App 和场景生效。
- 想要"最准最全"就得上 Xposed（需 root），否则在无障碍精度上接受折中。

关键利好：**因为是个人自用、自己 sideload 安装**，Google Play 对无障碍/短信权限的严格审核
对你完全不适用——这恰恰是商业自动记账 App 最头疼的部分。

务实路线建议（从易到难，可分阶段）：
1. **先做本体**：Room + Compose 的手动记账 App，跑通存储和 UI（学习价值高、确定能成）。
2. **加通知监听**：最简单的自动捕获，抓到带金额的通知就弹确认。
3. **加无障碍**：识别支付成功页/账单页，提升覆盖和精度（核心难点，最体现价值）。
4. **加短信兜底 + 账单导入**：补齐银行卡交易和历史账单。
5.（可选）**Xposed/Shizuku**：追求极致准确度时再上。
